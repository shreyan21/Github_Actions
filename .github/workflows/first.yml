on:
    push:
        branches: "*"

jobs:
    build:
        runs-on: ubuntu-latest

        outputs:
            author: "${{steps.one.outputs.Uploader}}"
            
        steps:
            - id: one
              run: |
                echo "Uploader=${{github.actor}}">>$GITHUB_OUTPUT

    deploy:
        needs: build
        runs-on: ubuntu-latest
        steps:
            - run: echo "${{needs.build.outputs.author}}"

        


               